<?xml version="1.0" encoding="utf-8"?>

<!--
    여행 리스트를 리사이클러뷰로 출력
-->

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/trip_list_item"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="5dp"
    android:paddingStart="10dp"
    android:paddingEnd="10dp"
    android:background="?attr/selectableItemBackground"
    >

    <CheckBox
        android:id="@+id/checkbox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <TextView
        android:id="@+id/checklist_description"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="5"
        android:gravity="center_vertical"
        android:text="여기에 체크리스트 나타남"
        style="@style/CheckList"
        />

    <ImageButton
        android:id="@+id/checklist_delete"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:scaleType="centerInside"
        android:padding="13dp"
        android:src="@drawable/checklist_delete"
        android:contentDescription="@string/checkDelete"
        style="@style/CheckDelete"/>

</LinearLayout>